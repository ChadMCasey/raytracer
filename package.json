{
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.9.5",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "compile": "rm -rf dist && tsc",
    "serve": "live-server . --entry-file=src/index.html"
  }
}
